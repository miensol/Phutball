<UserControl x:Class="EndGames.Shell.Views.GameOptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org" 
             xmlns:utils="clr-namespace:EndGames.Shell.Utils" 
             mc:Ignorable="d"              
             d:DesignHeight="358" d:DesignWidth="467">
    <Grid x:Name="LayoutRoot">
        <Grid.Resources>            
        </Grid.Resources>
    	<Grid.ColumnDefinitions>
    		<ColumnDefinition/>
    		<ColumnDefinition Width="10"/>
    		<ColumnDefinition/>
    	</Grid.ColumnDefinitions>
    	<Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10" />
    		<RowDefinition Height="Auto"/>
            <RowDefinition Height="10" />
    		<RowDefinition Height="Auto"/>
            <RowDefinition Height="10" />
    		<RowDefinition Height="Auto"/>
            <RowDefinition Height="10" />
    		<RowDefinition Height="Auto"/>
            <RowDefinition Height="10" />
    		<RowDefinition Height="Auto"/>
    	</Grid.RowDefinitions>
    	
        <TextBlock>
            Computer Player On
        </TextBlock>
        <CheckBox Grid.Column="2" />
        
        <TextBlock Grid.Row="2">
            Make move using:
        </TextBlock>
        <TextBlock Grid.Row="2" Grid.Column="2">
            Search depth
        </TextBlock>
        
        <Button Grid.Row="4">
            Dfs search
        </Button>
        <TextBox Grid.Column="2" Grid.Row="4">
            50    
        </TextBox>         
        <Button  Grid.Row="6">
            Bfs search
        </Button>
        <TextBox Grid.Row="6" Grid.Column="2">
            50
        </TextBox>

        <Button Grid.Row="8">
            Alpha-Beta
        </Button>
        <TextBox Grid.Row="8" Grid.Column="2">
            50
        </TextBox>
        
        <Grid Grid.Row="10" Grid.ColumnSpan="3">
            <StackPanel Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center">Width</TextBlock>
                <utils:NumericUpDown  
                    Minimum="7"
                    Maximum="21"
                    Change="2"
                 Value="{Binding Width, Mode=TwoWay}"
                />
                <TextBlock VerticalAlignment="Center">Height</TextBlock>
                <utils:NumericUpDown 
                    Minimum="7"
                    Maximum="23"
                    Change="2"
                    Value="{Binding Height, Mode=TwoWay}"
                />
                <Button x:Name="UpdateBoardSize" ToolTip="This will restart the game">Update</Button>
            </StackPanel>
        </Grid>
       
    </Grid>    
</UserControl>
